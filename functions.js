document.onkeydown = function(e) {
    switch (e.keyCode) {
        case 65:
            //document.getElementById('B').play();
            console.log("Key is not found!");
            break;
        default:
            console.log("Key is not found!");
    }
};